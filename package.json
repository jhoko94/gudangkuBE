{
  "name": "gudang-backend",
  "version": "1.0.0",
  "description": "Backend untuk prototipe Gudang Kita",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon index.js",
    "start": "node index.js",
    "build": "npm install && npx prisma generate && npx prisma migrate deploy && npm run start"
  },
  "dependencies": {
    "@prisma/client": "^5.10.2",
    "cors": "^2.8.5",
    "express": "^4.18.3",
    "prisma": "^5.10.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}